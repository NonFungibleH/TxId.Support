/**
 * The legal entity behind the product.
 *
 * TxID IS A PRODUCT, NOT A COMPANY, and the legal pages were written as though
 * it were one: "you agree to be bound", "our total liability", with no named
 * counterparty anywhere. An agreement whose other side is a brand name is an
 * agreement a customer's counsel will ask about, and the first time it matters
 * is the worst time to answer it.
 *
 * 3UILD is the incorporated entity. Every obligation, licence and liability on
 * /terms and /privacy attaches to it; TxID stays the name of the service being
 * provided. Defined ONCE here because the two pages must never disagree about
 * who the customer is contracting with, and because this changes: a dedicated
 * entity is planned, and when it exists this file is the only edit.
 */
export const LEGAL_ENTITY = "3UILD"

/** How the entity is first introduced, before the short form takes over. */
export const LEGAL_ENTITY_FULL = "3UILD, a United States company"

/** The product. Named separately so "the Service" is never ambiguous. */
export const PRODUCT = "TxID"

export const LEGAL_CONTACT = "team@txid.support"
